{
    "name": "emektup-rules-tests",
    "version": "1.0.0",
    "description": "Firestore Security Rules tests",
    "scripts": {
        "test": "jest",
        "test:emulator": "firebase emulators:exec --project emektup-staging \"jest\""
    },
    "dependencies": {
        "@firebase/rules-unit-testing": "^3.0.4",
        "firebase": "^10.8.0"
    },
    "devDependencies": {
        "firebase-tools": "^13.4.0",
        "jest": "^29.7.0"
    }
}